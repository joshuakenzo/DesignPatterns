<form-validation>

    <formset>
        <!-- Exemplo de definicao de validacoes para um formulario 
        <form name="NomeDaTela">
            <field property="cpf"
                depends="cpf">
                <arg0 key="dirigente.cpf"/>
            </field>

            <field property="processo"
                depends="numeroProcesso">
                <arg0 key="prestacaoContas.processoAdministrativo"/>
            </field>

            <field property="valorObrigatorio"
                depends="required,double">
                <arg0 key="convenio.valorObrigatorio"/>
            </field>

            <field property="valorOpcional"
                depends="double">
                <arg0 key="convenio.valorOpcional"/>
            </field>

            <field property="ano"
                depends="required,range">
                <arg0 key="convenio.ano"/>
                <arg1 name="range" key="${var:min}" resource="false" />
                <arg2 name="range" key="${var:max}" resource="false" />
                <var>
                    <var-name>min</var-name>
                    <var-value>1990</var-value>
                </var>
                <var>
                    <var-name>max</var-name>
                    <var-value>2003</var-value>
                </var>
            </field>
        </form> -->
    </formset>
    
</form-validation>